<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/p_amethyst</item>
        <item name="colorPrimaryDark">@color/p_dark_orchid</item>
        <item name="colorAccent">@color/p_orange_soda</item>
    </style>

</resources>
